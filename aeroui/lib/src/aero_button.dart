import 'package:flutter/widgets.dart';

class AeroButton extends StatelessWidget {
  const AeroButton({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
